pipeline {
    agent any

    stages {
        stage('Check Java Version') {
            steps {
                script {
                    sh "java --version"
                }
            }
        }
    }
}
